# WG-12 balance acceptance report — complete

## Regression evidence

- `BalanceTelemetryCheck` and `BalanceTelemetryIntegrationCheck`: passed. Telemetry is observational, rotates JSONL output, and records per-mechanic trigger count, damage contribution, generation depth, and peak events/second. The integration check covers `run_started` → `mutation_committed` → `run_finished` and advanced-crisis context.
- `FullBalanceAcceptanceCheck`: passed with 1,000 deterministic, side-effect-free standard-route seeds. It verifies 6–7 events, exactly 2 rewards, 2–3 beacons, the 3/5/8 Boss loadout floor, and prints the fixed-seed failure list; the list is `[]`.
- `MechanicRuntimeCheck`, `DashChargeBalanceCheck`, `FrenzyUptimeCheck`, `EquipmentMechanicCoverageCheck` (142 entries), `CombatBudgetCheck`, `SalvageQuotaCheck`, `EconomyServiceCheck`, `MetaProgressionCheck`, `AdvancedCrisisCheck`, and `RunContentContractCheck`: passed in focused regression runs.
- `PreflightRunIntegrationCheck`: passed. The eight canonical, data-driven calibrations correctly reach run state, including bulkhead, heat, debt, and four resonance candidates.
- `AdvancedCrisisRunIntegrationCheck`, `AdvancedCrisisExploreConfigCheck`, `AdvancedCrisisEnemySpawnCheck`, `AdvancedCrisisEventCostCheck`, and `AdvancedCrisisBossBudgetCheck`: passed. Advanced crisis is distinct from the in-run counter and reaches economic, exploration, enemy, Boss, and event consumers.
- `AdvancedCrisisBossRuntimeCheck`: passed using the live formal-run `StarColossusController`. Crisis 5 applies the 6,776 EHP budget and, on crossing 60% HP, applies its one-time 15% future-skill-cycle reduction. Crisis 9 additionally applies its 0.9 cooldown multiplier before that phase reduction (2.0 → 1.8 → 1.53 seconds).
- `git diff --check`: passed.

## Four-scenario automated acceptance report

All figures below use 1,000 fixed deterministic seeds. Duration includes routing, encounter, Boss, and shop-transition time; `Boss loadout P25` is the auxiliary count before each Boss.

| Scenario | P25 / P50 / P75 / P90 minutes | Boss loadout P25 | Pity triggers |
|---|---:|---:|---:|
| Whiteboard | 48.45 / 50.49 / 52.59 / 53.87 | 3 / 5 / 8 | 4,037 |
| Standard | 46.88 / 48.92 / 51.02 / 52.30 | 3 / 5 / 8 | 1,955 |
| Single-family highroll | 44.15 / 46.20 / 48.30 / 49.58 | 3 / 5 / 9 | 1,074 |
| Cross-family highroll | 43.53 / 45.57 / 47.67 / 48.95 | 3 / 5 / 10 | 920 |

The model explicitly generates 8–9 battles, 6–7 events, 2 rewards, and 2–3 beacons; it models stage income, 3/5/8 guarantees, dry-streak pity, and scenario-specific acquisition/Boss pacing. It does not mutate `RunManager` or write runtime game state.

## Manual handoff

The automated report establishes deterministic functional and balance evidence. A player-facing playthrough is still useful for subjective feel, visual readability, and input comfort; those are not represented as a pass/fail claim in this headless report.

Godot may emit ObjectDB/resource shutdown warnings after successful headless checks. No assertion failed; investigate separately if the warnings reproduce during a normal interactive session.
