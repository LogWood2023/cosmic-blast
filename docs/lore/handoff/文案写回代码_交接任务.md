# 交接任务 · 把新正典文案写回代码

> **执行者**：Sonnet（或任何接手的 agent）。
> **目标**：把 `docs/lore/` 已重构的新正典文案，逐条写回游戏代码里对应的文案字段，并通过 `tools/*_check.gd` 全部校验。
> **前置必读**（顺序）：`note/WORLDVIEW.md`（唯一剑权）→ `docs/lore/README.md`（风格基准 + 机制锚点）→ 对应的 `docs/lore/0X_*.md`。
> **本轮不做**：主线剧情脚本（未开工，另立任务）。

---

## 一、铁律：文案字段的硬约束（来自校验工具，违反即 fail）

`tools/equipment_copy_quality_check.gd` / `designed_enemy_copy_quality_check.gd` / `run_copy_quality_check.gd` / `ui_copy_quality_check.gd` 对写进代码的文案强制：

1. **必须含中文（CJK）**，且 **name 与 description 都不能为空**。
2. **禁止任何英文字母**（a–z / A–Z）。`docs/lore` 样例里出现的英文（家族键 `mining_barrage`、`colossus`，或 `run/drone/dash` 等）**只是文档索引，写回时必须全部去掉/改中文**。
3. **禁设计词**（`FORBIDDEN_COPY_TERMS`）：`适合 / 预留 / 测试 / 构筑 / 需求 / 玩家 / build / early / late / run / drone / dash / fire`。
   - 特别注意「**玩家**」被禁——正文一律用「你」。
   - 「构筑 / 需求」被禁——描述装备别用这两个词。
4. 写文案时**保持专有名词与正典完全一致**（席位名、亚种名、星髓矿、算力、天穹协议、方舟核心、引导之声、回声…）。

> 每改完一族/一批，立刻跑对应 check（见第四节），**红了当场修**，不要攒到最后。

---

## 二、映射表：docs/lore 文件 → 代码位置 → 校验工具

| 文档 | 代码文案位置 | 字段 | 校验工具 | 结构性注意 |
| --- | --- | --- | --- | --- |
| `05_装备风味文案.md` | `scripts/core/EquipmentCatalog.gd` | 每件装备的 `description` | `equipment_copy_quality_check.gd`（≥110 件） | 直接替换即可。文档是"样例库"，按家族基调改写现有 description，别照抄含英文的行 |
| `07_事件契约文案.md` | `scripts/core/RunManager.gd` | `EVENT_PROFILES` / `ORE_SOURCE_BIAS_PROFILES` / `RUN_CONDITION_PROFILES` / `REWARD_CACHE_CHOICE_PROFILES` / family_route 的 `description`/`flavor`/`room_effect_text`/`effects_text` | `event_contract_*`、`ore_source_*`、`run_condition_variety_check.gd`、`reward_cache_*`、`route_directive_*`、`run_copy_quality_check.gd` | 逐 key 对应，注意 `ark_medical_relay` 已改口径（临终关怀=神明使者，非天堂号）|
| `04_系统与UI文案.md` | 分散：`scripts/gameplay/explore/ExploreRoom.gd`、`MineralPickup.gd`、`ExploreReward.gd`、`scripts/ui/world_map/*Popup.gd`、`scripts/core/RunManager.gd` 等 | 各 UI 提示串 | `ui_copy_quality_check.gd`、`run_copy_quality_check.gd` | 引入「引导之声 / 方舟核心（系统）」双声线；战败=天穹协议清除记录 |
| `02_危机警报播报.md` | `scripts/app/WorldMap.gd` | 危机上升/锁定/返回中心的播报串 | `run_copy_quality_check.gd`（若覆盖） | 视角改「引导之声 + 天穹协议判定」；不提前戳破"拯救人类是谎"|
| `03_Boss登场与击败台词.md` | `scripts/entities/bosses/*.gd`（15 个 boss，`boss_name` 已就位） | **登场/半血/击败台词字段目前不存在** | — | ⚠️ **结构性**：需先加台词字段/播放系统，再填文案。见第三节 |
| `06_敌人图鉴文案.md` | `scripts/entities/designed_enemies/DesignedEnemyCatalog.gd` | 现只有 `mechanic`（战斗手感） | `designed_enemy_copy_quality_check.gd`（校验 `name`/`family`/`mechanic`）| ⚠️ **结构性**：图鉴"世界观档案"在代码里**没有字段**。见第三节 |

---

## 三、需要先决策的结构性问题（不是纯文本替换）

这两项动到数据结构，**先决定再动手**（可回报给项目负责人，或按建议默认执行）：

1. **`06` 敌人图鉴档案往哪写？**
   - 现状：`DesignedEnemyCatalog.gd` 每条只有 `mechanic`（打法手感），且 check 只验这个字段。`06` 写的是"它曾经是什么"的世界观档案，**当前无字段容身**。
   - 建议：给每条加一个 `codex`（或 `flavor`）字段承载 `06` 的档案文本，并在 `designed_enemy_copy_quality_check.gd` 增加对该字段的同规则校验；`mechanic` 保持不动（它是打法说明，本就该和世界观分开）。
   - 若不想动结构：`06` 暂留作文档，代码侧只需确认 25 条 `mechanic` 不与新正典冲突（现有 mechanic 是中性战斗描述，基本无需改）。

2. **`03` Boss 台词怎么进游戏？**
   - 现状：boss 脚本只有 `boss_name` 和进场黑幕白字（只显示名字）。登场/半血/击败台词**没有字段、也没有播放时机**。
   - 建议：在 boss 基类/各控制器加 `intro_line` / `half_hp_line` / `defeat_line` 三个 `@export`，并在进场结束、半血、`_die()` 时触发一个简单的台词浮层（可复用现有 `_setup_intro_overlay` 那套 CanvasLayer）。文案照 `03` 填。
   - 这是有工作量的功能开发，建议**单独排期**，别和纯文本替换混在一起。

> 结论：**先做纯文本替换（05 / 07 / 04 / 02）**——这些字段都已存在，改完就能过 check、直接见效；**06 / 03 涉及加字段/加系统，单独处理**。

---

## 四、验证方式

每个 `tools/xxx_check.gd` 是独立校验脚本。按项目既有方式跑（Godot headless 执行对应 check 脚本/场景），确认输出 **PASS / 无 fail**。至少跑：

- `equipment_copy_quality_check.gd`（改完 05）
- `run_copy_quality_check.gd` + `ui_copy_quality_check.gd`（改完 04 / 02）
- `event_contract_effect_check.gd` / `event_contract_depth_check.gd` / `run_condition_variety_check.gd` / `reward_cache_choice_check.gd` / `ore_source_profile_check.gd` / `route_directive_check.gd`（改完 07）
- `designed_enemy_copy_quality_check.gd`（若给 06 加了字段）

---

## 五、执行顺序与勾选

- [x] 读齐前置正典（WORLDVIEW → README → 对应 0X）
- [~] **05 装备**：`EquipmentCatalog.gd` 全部 `description` 已在库中，本轮完成合规校对（见第六节），**未跑 check（本机无 Godot）**
- [~] **07 事件**：`RunManager.gd` 各 PROFILES 文案已在库中，本轮完成合规校对并修正 7 处禁用词，**未跑 check**
- [~] **04 系统/UI**：现有 UI 提示串已校对并修正 2 处；⚠️ **双声线（引导之声/方舟核心系统）的死亡/重启专章文案尚未写回代码**（见第六节）
- [ ] **02 危机播报**：⚠️ **doc 02 的播报文案从未写回代码**，`WorldMap.gd` 里不存在；这是待开发功能，非校对可解决（见第六节）
- [ ] **06 图鉴**（结构决策后）：加 `codex` 字段 + 校验，或暂留文档 —— 仍未加字段
- [ ] **03 Boss台词**（功能排期后）：加台词字段 + 浮层触发，填 `03` 文案 —— 仍未加字段

> 勾选图例：`[x]` 完成 · `[~]` 已写回且本轮校对通过（仅差 headless check 复核）· `[ ]` 未落地。

---

## 六、校对结论（2026-07-09 一轮静态校对 + 修正）

> 本机未安装 Godot，无法执行 `tools/*_check.gd` 的 headless 校验；本节结论均由**静态通读代码 + 全文正则搜索**得出。合入后请务必按第四节补跑对应 check 做最终确认。

### 6.1 已直接修正（12 处 / 5 文件，git working tree 未提交）

| 范围 | 文件:行 | 修改前 → 修改后 | 违规 |
| --- | --- | --- | --- |
| 05 | `EquipmentCatalog.gd:114` | "…服务引力流派。" → "…在引力扭曲中咬得更紧。" | 设计视角残留 |
| 05 | `EquipmentCatalog.gd:525` | "…巨构流派的入门组件。" → "…巨构标准列装的撞击组件。" | 设计视角残留 |
| 05 | `EquipmentCatalog.gd:825` | "…整片屏幕被…" → "…整片空域被…" | 第四面墙（"屏幕"） |
| 07 | `RunManager.gd:33` | "…适合作为任何装配的补给底盘。" → "…可作为任何…" | 禁用词"适合" |
| 07 | `RunManager.gd:42` | "…适合在奖励航线里囤积…" → "…常在奖励航线里囤积更多…" | 禁用词"适合" |
| 07 | `RunManager.gd:800` | "…变得更像一个构筑。" → "…战术路数更加自成一体。" | 禁用词"构筑" |
| 07 | `RunManager.gd:809` | "…能强化冲锋构筑。" → "…能让冲锋打法更快成形。" | 禁用词"构筑" |
| 07 | `RunManager.gd:859` | "…推向火力覆盖构筑。" → "…推向火力覆盖纹章。" | 禁用词"构筑" |
| 07 | `RunManager.gd:4750`（`_route_plan_tactic_hint` 的 `suffix`，拼进 `tactic_hint`） | "奖励航线适合补强核心纹章。" → "奖励航线正好用来补强核心纹章。" | 禁用词"适合" |
| 04 | `ExploreRoom.gd:1021` | "正在决定玩家出生点..." → "正在校准出生坐标..." | 禁用词"玩家" |
| 04 | `EventResultPopup.gd:30` | "航图层级：T%d" → "航图层级：第%d层" | 残留英文字母 |
| 06 | `DesignedEnemyCatalog.gd:11` | "…适合在混战中补上致命火线。" → "…总能在混战中补上致命火线。" | 禁用词"适合" |

> 注：`ORE_SOURCE_BIAS_PROFILES` 的 `hint` 字段与敌人 `mechanic` 字段目前**不被任何 check 脚本校验**，但同样违反第一节铁律的禁用词规则，已一并修掉。

### 6.2 已核对、判定合规、未改动
- **专有名词零旧设定残留**：全库未见"医疗乐园""中立仲裁""工程师造的第六方案"等旧口径；五席家族键（colossus/warped/hell_eye/divine/paradise）与中文名对应全部一致。
- **`ark_medical_relay` 口径正确**：现为"方舟核心接管残留医疗协议"的中性表述，**没有**误挂天堂号，与"临终关怀=神明使者"不冲突。（仅文案偏薄、未显式点出神明使者背景，属可选加厚，非纠错。）
- **05 装备**通过四项硬指标（含 CJK、零英文字母、零禁用词、零旧设定残留）。
- **02 现有零散危机 UI 标签**（`WorldMap.gd:479/482/535/799`、`RunManager.gd:2336` 的"警报锁定/迎战危机"等）全部合规。

### 6.3 ⚠️ 关键发现：两处"文档已定稿，但从未写回代码"
校对时才发现，以下两块并非"文案有瑕疵待改"，而是**代码里根本没有对应文案**（全库关键词搜索零命中，且与执行勾选表 `[ ]` 状态吻合）：

1. **02 危机警报播报**：`docs/lore/02` 里的危机 +1 随机播报、5/12/21 三级锁定判定、返回中心提示等，`WorldMap.gd` 一个字都没有。需要**新增播报触发点 + 随机抽取逻辑 + 播报层**才能落地——这是有开发量的功能实现，建议单独排期，别当成"改文案"。
2. **04 双声线死亡/重启专章**："引导之声 / 方舟核心系统"双声线的战败·苏醒·重启台词全库零命中（`GameOver.gd`/`EvacuationSuccessHUD.gd` 文案干净但只有"回声"术语、无"引导之声"人声）。同样需要先决定挂载点再写回。

### 6.4 结构性待办仍未动（与本文档第三节一致）
- **03 Boss 台词**：boss 脚本仍只有 `boss_name`，无 `intro_line/half_hp_line/defeat_line` 字段与播放时机（`DivineMessenger.gd` 的 `_setup_intro_overlay` 那套 CanvasLayer 可复用）。
- **06 敌人图鉴档案**：`DesignedEnemyCatalog.gd` 每条仍只有 `mechanic`，无 `codex/flavor` 字段承载世界观档案。

### 6.5 交给文案 AI 的待润色清单（本轮未改，属风格判断题，非硬违规）
- `EquipmentCatalog.gd` 中"手感""效率很高"等偏测评/说明书口吻；`armor_lattice`（机制是减伤 `damage_taken_mult`）用"输出结构"描述，疑似机制与文案对不上，请策划核实机制意图后定稿。
- `AUXILIARY_EXPANSION_ROWS` 约 30 件"填充款"装备文风比 `05` 样例更平铺直叙，缺各家族"初衷→异化"的反讽底色（如神使系"抹去自我加入合唱"的暗黑意味多数未体现）。可选做一轮创作性润色。
- `ark_medical_relay` 事件可加一句神明使者临终关怀背景钩子，贴合正典叙事。

### 6.6 附带发现（非文案，已单独派工）
探索房间斜杠指令控制台（`/清测试敌` 等，`ExploreRoom.gd` 约 583-870 行）**无 `OS.is_debug_build()` 保护**，发布版里任何人按 Enter 即可唤出并刷怪/清怪。属工程遗留，已另立独立任务处理，不在本文案批次内。

---

*本文件由设定重构环节产出，第六节为 2026-07-09 校对轮补记。文案基调、专有名词、家族对应，一切以 `note/WORLDVIEW.md` 为准。*
